---
import type { HTMLAttributes } from 'astro/types'
import { cn } from '@/lib/utils'

export type Props = HTMLAttributes<'figure'>

const { class: className, ...props } = Astro.props
---

<figure class={cn(className)} {...props}>
  <slot />
</figure>
