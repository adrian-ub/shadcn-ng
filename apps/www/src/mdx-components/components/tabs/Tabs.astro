---
import { Tabs as ComponentTabs } from '@/components/ui/tabs'
export { type Props } from '@/components/ui/tabs/Tabs.astro'
import { cn } from '@/lib/utils'

const { class: className, ...rest } = Astro.props
---

<ComponentTabs class={cn('relative mt-6 w-full', className)} {...rest}>
  <slot />
</ComponentTabs>
