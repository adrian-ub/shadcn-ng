---
import type { HTMLAttributes } from 'astro/types'
import { cn } from '~/lib/utils'

export type Props = HTMLAttributes<'img'>

const { class: className, ...props } = Astro.props
---

<img class={cn('rounded-md', className)} {...props} />
